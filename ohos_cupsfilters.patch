diff --git a/cupsfilters/image-bmp.c b/cupsfilters/image-bmp.c
index c726278f..3a3002ed 100644
--- a/cupsfilters/image-bmp.c
+++ b/cupsfilters/image-bmp.c
@@ -76,7 +76,7 @@ _cupsImageReadBMP(
   cups_ib_t	*in,			/* Input pixels */
 		*out,			/* Output pixels */
 		*ptr;			/* Pointer into pixels */
-  cups_ib_t	colormap[256][4];	/* Colormap */
+  cups_ib_t	colormap[256][4] = {0};	/* Colormap */
 
 
   (void)secondary;
@@ -312,8 +312,18 @@ _cupsImageReadBMP(
 
             if (bit == 0xf0)
 	    {
-              if (color < 0)
-		temp = getc(fp);
+            if (color < 0)
+			{
+				temp = getc(fp);
+				if (temp == EOF)
+				{
+					fputs("DEBUG: Failed to read data!\n", stderr);
+    				free(in);
+					free(out);
+    				fclose(fp);
+    				return (1);
+				}
+			}
 	      else
 		temp = color;
 
@@ -408,7 +418,17 @@ _cupsImageReadBMP(
 	    */
 
             if (color < 0)
-	      temp = getc(fp);
+			{
+				temp = getc(fp);
+				if (temp == EOF)
+				{
+					fputs("DEBUG: Failed to read data!\n", stderr);
+					free(in);
+					free(out);
+					fclose(fp);
+					return (1);
+				}
+			}
 	    else
 	      temp = color;
 
diff --git a/cupsfilters/image-gif.c b/cupsfilters/image-gif.c
index c225dc70..36d4e43e 100644
--- a/cupsfilters/image-gif.c
+++ b/cupsfilters/image-gif.c
@@ -73,7 +73,7 @@ _cupsImageReadGIF(
     const cups_ib_t *lut)		/* I - Lookup table for gamma/brightness */
 {
   unsigned char	buf[1024];		/* Input buffer */
-  gif_cmap_t	cmap;			/* Colormap */
+  gif_cmap_t	cmap = {0};			/* Colormap */
   int		i,			/* Looping var */
 		bpp,			/* Bytes per pixel */
 		gray,			/* Grayscale image? */
diff --git a/cupsfilters/image-jpeg.c b/cupsfilters/image-jpeg.c
index a93fe92d..98a19842 100644
--- a/cupsfilters/image-jpeg.c
+++ b/cupsfilters/image-jpeg.c
@@ -178,6 +178,17 @@ _cupsImageReadJPEG(
   in  = malloc(img->xsize * cinfo.output_components);
   out = malloc(img->xsize * cupsImageGetDepth(img));
 
+  if (!in || !out)
+  {
+    fprintf(stderr, "DEBUG: Failed to allocate memory for JPEG decoding.\n");
+    free(in);
+    free(out);
+
+    jpeg_destroy_decompress(&cinfo);
+    fclose(fp);
+    return (1);
+  }
+
   jpeg_start_decompress(&cinfo);
 
   while (cinfo.output_scanline < cinfo.output_height)
diff --git a/cupsfilters/image-photocd.c b/cupsfilters/image-photocd.c
index 073046ec..07de8748 100644
--- a/cupsfilters/image-photocd.c
+++ b/cupsfilters/image-photocd.c
@@ -76,6 +76,12 @@ _cupsImageReadPhotoCD(
   * Allocate and initialize...
   */
 
+  if (primary == CUPS_IMAGE_CMYK)
+  {
+    fputs("DEBUG: colorspace is wrong!\n", stderr);
+    fclose(fp);
+    return (1);
+  }
   img->colorspace = (primary == CUPS_IMAGE_RGB_CMYK) ? CUPS_IMAGE_RGB : primary;
   img->xppi       = 200;
   img->yppi       = 200;
diff --git a/cupsfilters/image-sun.c b/cupsfilters/image-sun.c
index 989d039f..d085bc90 100644
--- a/cupsfilters/image-sun.c
+++ b/cupsfilters/image-sun.c
@@ -140,7 +140,7 @@ _cupsImageReadSunRaster(
   if (scanwidth & 1)
     scanwidth ++;
 
-  if (ras_depth < 24 && ras_maplength == 0)
+  if (ras_depth <= 8 && ras_maplength == 0)
   {
     img->colorspace = secondary;
     in = malloc(img->xsize + 1);
diff --git a/cupsfilters/image-tiff.c b/cupsfilters/image-tiff.c
index 5fe89071..1a784e13 100644
--- a/cupsfilters/image-tiff.c
+++ b/cupsfilters/image-tiff.c
@@ -332,6 +332,19 @@ _cupsImageReadTIFF(
     out = malloc(img->ysize * bpp);
   }
 
+  if (!in || !out)
+  {
+    fputs("DEBUG: Failed to allocate memory for TIFF decoding.\n", stderr);
+	  free(in);
+	  free(out);
+
+    _TIFFfree(scanline);
+	  TIFFClose(tif);
+	  
+	  fclose(fp);
+	  return (1);
+  }
+
  /*
   * Read the image.  This is greatly complicated by the fact that TIFF
   * supports literally hundreds of different colorspaces and orientations,
diff --git a/cupsfilters/image-zoom.c b/cupsfilters/image-zoom.c
index 5896665f..e19e22bc 100644
--- a/cupsfilters/image-zoom.c
+++ b/cupsfilters/image-zoom.c
@@ -25,7 +25,7 @@
  */
 
 #include "image-private.h"
-
+#include <limits.h>
 
 /*
  * Local functions...
@@ -90,8 +90,10 @@ _cupsImageZoomNew(
   cups_izoom_t	*z;			/* New zoom record */
   int		flip;			/* Flip on X axis? */
 
+  if (xsize == INT_MIN)
+    return (NULL);
 
-  if (xsize > CUPS_IMAGE_MAX_WIDTH ||
+  if (abs(xsize) > CUPS_IMAGE_MAX_WIDTH ||
       ysize > CUPS_IMAGE_MAX_HEIGHT ||
       (xc1 - xc0) > CUPS_IMAGE_MAX_WIDTH ||
       (yc1 - yc0) > CUPS_IMAGE_MAX_HEIGHT)
diff --git a/filter/imagetoraster.c b/filter/imagetoraster.c
index fcc6cb1e..be9abe03 100644
--- a/filter/imagetoraster.c
+++ b/filter/imagetoraster.c
@@ -38,7 +38,7 @@
 #include <math.h>
 #include <signal.h>
 #include <string.h>
-
+#include <limits.h>
 
 /*
  * Globals...
@@ -1144,23 +1144,57 @@ main(int  argc,				/* I - Number of command-line arguments */
     * Set the bitmap size...
     */
 
-    header.cupsWidth  = width * header.HWResolution[0] / 72.0;
-    header.cupsHeight = length * header.HWResolution[1] / 72.0;
+	unsigned long long width_check = (unsigned long long)width * header.HWResolution[0];
+	unsigned long long height_check = (unsigned long long)length * header.HWResolution[1];
+	if (width_check > UINT_MAX || height_check > UINT_MAX)
+	{
+	  fputs("ERROR: Integer overflow in cupsWidth/cupsHeight calculation.\n", stderr);
+	  ppdClose(ppd);
+	  return (1);
+	}
+    header.cupsWidth  = (unsigned)(width_check / 72.0);
+	header.cupsHeight = (unsigned)(height_check / 72.0);
   } else {
    /*
     * Set the bitmap size...
     */
 
-    header.cupsWidth  = (Orientation & 1 ? yprint : xprint) *
+	unsigned long long cups_width_check = (unsigned long long)(Orientation & 1 ? yprint : xprint) *
       header.HWResolution[0];
-    header.cupsHeight = (Orientation & 1 ? xprint : yprint) *
+	unsigned long long cups_height_check = (unsigned long long)(Orientation & 1 ? xprint : yprint) *
       header.HWResolution[1];
+	if (cups_width_check > UINT_MAX || cups_height_check > UINT_MAX)
+	{
+	  fputs("ERROR: Integer overflow in cupsWidth/cupsHeight calculation.\n", stderr);
+	  ppdClose(ppd);
+	  return (1);
+	}
+    header.cupsWidth  = (unsigned)cups_width_check;
+	header.cupsHeight = (unsigned)cups_height_check;
   }
-  header.cupsBytesPerLine = (header.cupsBitsPerPixel *
-			     header.cupsWidth + 7) / 8;
+
+  unsigned long long bpl_check = (unsigned long long)header.cupsBitsPerPixel *
+			     header.cupsWidth + 7;
+  if (bpl_check > UINT_MAX)
+  {
+	fputs("ERROR: Integer overflow in cupsBytesPerLine calculation.\n", stderr);
+	ppdClose(ppd);
+	return (1);
+  }
+  header.cupsBytesPerLine = (unsigned)((bpl_check) / 8);
 
   if (header.cupsColorOrder == CUPS_ORDER_BANDED)
-    header.cupsBytesPerLine *= header.cupsNumColors;
+  {
+	unsigned long long bpl_banded = (unsigned long long)header.cupsBytesPerLine *
+			     header.cupsNumColors;
+	if (bpl_banded > UINT_MAX)
+	{
+	  fputs("ERROR: Integer overflow in cupsBytesPerLine (BANDED mode).\n", stderr);
+	  ppdClose(ppd);
+	  return (1);
+	}
+	header.cupsBytesPerLine = (unsigned)bpl_banded;
+  }
 
   header.Margins[0] = PageLeft;
   header.Margins[1] = PageBottom;
@@ -1392,7 +1426,21 @@ main(int  argc,				/* I - Number of command-line arguments */
   fprintf(stderr, "DEBUG: cupsColorSpace = %d\n", header.cupsColorSpace);
   fprintf(stderr, "DEBUG: img->colorspace = %d\n", img->colorspace);
 
+  if (header.cupsBytesPerLine > UINT_MAX / 2)
+  {
+    fputs("ERROR: Integer overflow in row buffer allocation.\n", stderr);
+    cupsImageClose(img);
+    ppdClose(ppd);
+    return (1);
+  }
   row = malloc(2 * header.cupsBytesPerLine);
+  if (row == NULL)
+  {
+    fputs("ERROR: Unable to allocate memory for row buffer.\n", stderr);
+    cupsImageClose(img);
+    ppdClose(ppd);
+    return (1);
+  }
   ras = cupsRasterOpen(1, CUPS_RASTER_WRITE);
 
   for (i = 0, page = 1; i < Copies; i ++)
@@ -1437,6 +1485,15 @@ main(int  argc,				/* I - Number of command-line arguments */
 	    z = _cupsImageZoomNew(img, xc0, yc0, xc1, yc1, xtemp, ytemp,
 	                          Orientation & 1, zoom_type);
 
+		  if (z == NULL)
+		  {
+			  fputs("ERROR: Unable to create image zoom record.\n", stderr);
+			  free(row);
+  			  cupsRasterClose(ras);
+  			  cupsImageClose(img);
+  			  ppdClose(ppd);
+             return (1);
+		  }
          /*
 	  * Write leading blank space as needed...
 	  */
